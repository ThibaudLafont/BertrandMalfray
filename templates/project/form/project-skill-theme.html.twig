{% block SkillType_widget %}

    <table>
        <tr>
            <td>
                {{ form_row(form.position) }}
            </td>
        </tr>

        <tr>
            <td>

                {{ form_row(form.content) }}
            </td>
        </tr>
    </table>

{% endblock %}